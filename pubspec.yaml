name: card_album_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.1
  image: ^3.2.2
  cupertino_icons: ^1.0.8
  google_mlkit_text_recognition: ^0.15.0
  tflite_flutter: ^0.9.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/card_detector.tflite